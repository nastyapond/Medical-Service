[pytest]
# Add the workspace root to the Python path so pytest can find the app module
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
